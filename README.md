# Rapid Crisis Response

Dashboard UI for emergency monitoring. Copy `.env.example` to `.env` and configure Firebase when the backend is ready.

## Scripts

- `npm install` — install dependencies
- `npm run dev` — local dev server
- `npm run build` — production build

Add `public/sounds/alert.mp3` and `public/sounds/notification.mp3` for audio alerts (replace placeholders if needed).
